!!! Strict
%html{html_attrs('en')}
  %head
    %title= @title.nil? ? "" : "#{@title}"
    = stylesheet_link_tag 'styles.css'
    = javascript_include_tag :defaults
  %body
    #header
      %h1= @title.nil? ? "" : "#{@title}"
    #main
      = yield